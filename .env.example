# ── Public (inlined into static bundle at build time) ─────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_BASE_PATH=/Expense
NEXT_PUBLIC_INVITE_CODE=your-secret-invite-code

# ── GitHub Actions Cron Secrets (NOT in static bundle) ────────────
# These are stored ONLY in GitHub Secrets, never in .env.local
# SUPABASE_EDGE_BASE_URL=https://your-project.supabase.co/functions/v1
# SUPABASE_CRON_SECRET=your-cron-secret

# ── Supabase Edge Function Secrets (set in Supabase Dashboard) ────
# SUPABASE_SERVICE_ROLE_KEY=...  (never in frontend)
# X_CRON_SECRET=...              (must match SUPABASE_CRON_SECRET)
